import React from 'react';
import ReactDOM from 'react-dom';
import App from './App/App';

// Mounting our React extension to a DOM element
ReactDOM.render(<App />, document.getElementById('substrate'));
